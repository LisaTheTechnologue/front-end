export interface TripImage {
  id: number;
  name: string;
  url: string;
  type: string;
  size: number;
  dataUpload:any;

  // grow: 1;
  // shrink: 0;
  // basis: 'calc(25% - 0.5rem)';
    // private byte[] picByte;
}
